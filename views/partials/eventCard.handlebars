<div class="row">
            <div class="column is-8">
                <h5 class="title is-5">
                    <a href="/event/{{event.event.id}}">{{event.event.title}}</a>
                </h5>
                <p>Date of event: {{event.event.date_created}}</p>
                <p>Hosted by: {{event.event.user.name}}</p>
                <p>{{event.event.event_info}}</p>
            </div>
            <br>
            <br>
            <div class="columns">
                <div class="column is-2 is-offset-1">
                    <button class="button is-link is-focused">RSVP</button>
                </div>
                <br>
                <br>
                <div class="column is-2">
                    <button class="button is-link is-focused">Share</button>
                </div>
            </div>
        </div>