<main>
    <div class="row">
                {{!-- <div class="col s12">
                    <ul class="tabs">
                        <li class="tab col s6"><a href="#accepted"> Friends </a></li>
                        <li class="tab col s6"><a href="#pending"> Pending </a></li>
                    </ul>
                </div> --}}
                <div id="accepted" class="container delete-btn">
                    {{#each friend}}
            
                        {{#friends}}
                    
                        <div class="columns box ">
                            <div class="column is-8 is-offset-1">
                                <span class="title">{{this.name}}</span>
                                <p>Friend ID: {{this.id}}</p>
                                {{!-- <p>Status: {{this.status}}</p> --}}
                            </div>
                            <div class="column">
                                <button class="button is-link is-focused delete-post-btn" data-value="{{this.id}}">Delete Friend</button>
                            </div>
                        </div>
                        <br>
                    
                    {{/friends}}
                    
                        {{else}}
                    <div class="row">
                        <div class="column is-10 is-offset-1">
                            <h4 class="title is-4"> You have no Friends! Let's add some! </h4>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>
            <div class="row">
                <div class="column is-10 is-offset-1">
                    <form id="input2">
                        <div class="field">
                            <label class="label-icon" for="search"><i class="material-icons">Add your friend by ID</i></label>
                            <input class="input search" type="text" placeholder="Input your friend's ID" id="input1">
                        </div>
                    </form>
                </div>
            </div>
        </form>
    </div>
</main>

<script src="/js/add-delete-friends.js"></script>